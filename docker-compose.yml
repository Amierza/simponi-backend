services:
  app:
    build: .
    container_name: simponi-backend
    env_file:
      - .env
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped